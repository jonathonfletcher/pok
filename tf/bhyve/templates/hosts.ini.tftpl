# GENERATED by tf/bhyve (tofu) — do not edit by hand.
# bhyve nodes are static DHCP reservations on the LAN, reached directly (no bastion).
# Node names match the Kubernetes node names / DNS (k8sm*/k8sw*.homelab.lan).

[primary_cp]
%{ for name, ip in primary_cp ~}
${name} ansible_host=${ip}
%{ endfor ~}

[secondary_cp]
%{ for name, ip in secondary_cp ~}
${name} ansible_host=${ip}
%{ endfor ~}

[workers]
%{ for name, ip in workers ~}
${name} ansible_host=${ip}
%{ endfor ~}

[control_plane:children]
primary_cp
secondary_cp

[kube_cluster:children]
control_plane
workers
